//Suma
//Resta
//Multiplicacion
//División

const calculator = {
    add: (a,b) => a+b,
    sub: (a,b) => a-b,
    mul: (a,b) => a*b,
    div: (a,b) => a/b,
}

module.exports = calculator;

//Prueba
// console.log(calculator.add(2,2)); calculator.add(2,2)